I want you to create a web-based study planner that I will host on github pages
The planner needs to use HTML/JS/CSS and VeuJS only, store any needed values locally (as cookies) and supports downloading output files (CSVs for tables, and everything as json for retrieval and plan updating), and uploading the json file to work on
The planner will need access openAI API for any ai access. the software will need to ask the user for their API key and store it locally as it will not be stored in the repository. Create a separate file with the prompt needed so I can edit it separately
First read the studyPlannerLogicSpecs.md as it has detailed information about the logic of this tool, then the studyPlannerUISpecs.md for some of the UI requirements
use the UI theme in 'designSystem.png' as the design system for this project
